console.log("ex3.js loaded...");

/**
 * Function "max" with 2 parameteres: 
 * @param {number} nr1 The first number to add
 * @param {number} nr2 The second number to add
 * @returns the maximum between them
 */

function max(nr1, nr2) {
    if(nr1 > nr2) {
        return nr1;
    }{ 
    return nr2;
    }
}